class deal {
    constructor(id, name, detail, price, uri, ratings) {
        this.id = id; // unique & number
        this.name = name; // string
        this.detail = detail; //string
        this.uri = uri; // string
        this.price = price; // float
        this.discount = discount; // num
    }
}

export default deal;
