class Rating {
    constructor(email, username, password, rating, desc) {
        this.email = email;
        this.username = username;
        this.password = password;
        this.rating = rating;
        this.desc = desc;
    }
}

export default Rating;
