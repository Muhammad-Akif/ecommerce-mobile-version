import React from 'react';
import { Text, View, StyleSheet } from 'react-native';

const Cart = props => {
    return (
        <View />
    );
}

export default Cart;

const styles = StyleSheet.create({

})
